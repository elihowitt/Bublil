#version 140

attribute vec2 position;
attribute vec2 texCoord;

varying vec2 texCoord0;

uniform vec2 scale;
uniform vec2 pos;

void main()
{
	vec2 finalPos = position * scale;
	gl_Position = vec4(finalPos + pos,0,0);
	texCoord0 = texCoord;
}