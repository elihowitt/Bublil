#version 140

attribute vec3 position;
attribute vec2 texCoord;

varying vec2 texCoord0;

uniform vec2 scale;
uniform vec2 pos;

void main()
{
	vec3 finalPos = vec3(position.x * scale.x, position.y * scale.y, 1);
	gl_Position = vec4((finalPos + pos).xy,1 ,1);
	texCoord0 = texCoord;
}