#pragma once

#include<string>

namespace render
{
	class FontRendere
	{
	public:
		FontRendere(const std::string& fontFile, const unsigned int& numChars, const std::string& shaderFile);
		~FontRendere();
	private:
		
	};
}